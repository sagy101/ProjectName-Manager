name: PR Test Verification (Legacy - Jobs moved to separate workflows)

on:
  pull_request:
    branches: [ main ]

permissions:
  pull-requests: write
  issues: write
  contents: read

jobs:
  placeholder:
    name: Placeholder Job
    runs-on: ubuntu-latest
    steps:
      - name: Workflow Migration Notice
        run: |
          echo "This workflow has been split into separate files:"
          echo "- Jest tests: .github/workflows/jest-tests.yml"
          echo "- E2E tests: .github/workflows/e2e-tests.yml"
          echo "This file can be deleted once the new workflows are confirmed working." 